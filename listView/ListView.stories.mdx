import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { ListView } from './ListView';
import ListItem from '../listItem/ListItem';
import { withKnobs, text, boolean, number } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Client } from '../Personas/assets';
import {TabBarProvider} from '../tabBarContext/TabBarProvider'

<Meta title="Component Lib| ListView" conponent={ListView} />

<Preview>
    <Story name="List View">
        <TabBarProvider>
        <ListView>
            <ListItem
                leftPersona={true}
                primaryText="Topic Name"
                rightIconName="radiobutton"
                onEnter={()=>{console.log("listitem 1 pressed")}}></ListItem>
            <ListItem
                leftIcon={true}
                leftIconName="share"
                primaryText="Share"
                rightIconName="arrowbutton"
                onEnter={()=>{console.log("listitem 2 pressed")}}></ListItem>
            <ListItem
                leftPersona={true}
                leftPersonaSrc={[Client]}
                primaryText="First Name"
                secondaryText="Email or Phone"
                rightIconName="checkbox"
                listSize="Medium"
                onEnter={()=>{console.log("listitem 3 pressed")}}></ListItem>
            <ListItem
                primaryText="Main Title"
                secondaryText="Label"
                rightIconName="arrowbutton"
                onEnter={()=>{console.log("listitem 4 pressed")}}></ListItem>
        </ListView>
        </TabBarProvider>
    </Story>
</Preview>
