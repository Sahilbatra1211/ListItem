import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { ListView } from './ListView';
import ListItemCheckbox from '../listItemCheckbox/ListItemCheckbox';
import ListItemArrowButton from '../listItemArrowButton/ListItemArrowButton';
import { withKnobs, text, boolean, number } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Client } from '../Personas/assets';
import {TabBarProvider} from '../tabBarContext/TabBarProvider'

<Meta title="Component Lib| ListView" conponent={ListView} />

<Preview>
    <Story name="List View">
        <TabBarProvider>
        <ListView>
            <ListItemCheckbox
                leftPersona={true}
                primaryText="Topic Name"
                rightIconName="radiobutton"
                onEnter={()=>{console.log("listitem 1 pressed")}}></ListItemCheckbox>
            <ListItemCheckbox
                leftIcon={true}
                leftIconName="share"
                primaryText="Share"
                rightIconName="arrowbutton"
                onEnter={()=>{console.log("listitem 2 pressed")}}></ListItemCheckbox>
            <ListItemCheckbox
                leftPersona={true}
                leftPersonaSrc={[Client]}
                primaryText="First Name"
                secondaryText="Email or Phone"
                rightIconName="Checkbox"
                listSize="Medium"
                onEnter={()=>{console.log("listitem 3 pressed")}}></ListItemCheckbox>
            <ListItemCheckbox
                primaryText="Main Title"
                secondaryText="Label"
                rightIconName="arrowbutton"
                onEnter={()=>{console.log("listitem 4 pressed")}}></ListItemCheckbox>
            <ListItemArrowButton leftIcon={true} leftIconName="blocked" primaryText="Blocked"></ListItemArrowButton>
        </ListView>
        </TabBarProvider>
    </Story>
</Preview>
