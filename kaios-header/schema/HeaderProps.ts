export interface headerProps {
    /**Describes the text of header */
    text?: string;
    /**Describes the theme of header */
    theme?: 'white' | 'grey' | 'dark';
    /**Describes the height of header */
    base?: 'normal' | 'large';
}
